# Photo Display - Uploader

Allows the user to upload an image file to the server for processing.

## Development

* run `npm start` to start the webpack development server

## Tests

* To run automated tests, run `npm test`
